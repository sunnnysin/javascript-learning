console.log('satyam');

let js = 'amazing';
console.log(js);

let firstName = 'Satyam';
let lastName = 'Kumar';
console.log(firstName, lastName);

// variables should not start with numbers and vaiables should only contain dollar and underscore,
// variable should not have space in between,
// variables which never gona change can be used in upper case
// we must use camelCase for naming variables,

let first$Name = 'Sunny';
let last_name = 'Singh';

let age43 = 28;
let RED = '#ff0000';

console.log(first$Name, last_name, age43, RED);